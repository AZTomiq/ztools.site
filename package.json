{
  "name": "ztools-site",
  "version": "1.0.0",
  "description": "Multi-tool utility site",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "build:secure": "node build.js --obfuscate",
    "dev": "node build.js && npx serve dist",
    "serve": "npx serve dist"
  },
  "keywords": [
    "tools",
    "utility",
    "calculator"
  ],
  "author": "Anph",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.9",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "javascript-obfuscator": "^4.0.0",
    "terser": "^5.0.0",
    "clean-css-cli": "^5.0.0",
    "html-minifier-terser": "^7.0.0"
  }
}