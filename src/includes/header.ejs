<header class="site-header">
  <div class="header-inner container">
    <a href="<%= rootPath %><%= locale %>/" class="logo">
      <span class="logo-icon">🛠️</span>
      <span class="logo-text">ZTools <span class="logo-sub">
          <%= category %>
        </span></span>
    </a>
    <nav class="main-nav">
      <ul>
        <li><a href="<%= rootPath %><%= locale %>/#job-income" class="<%= currentPath === 'tax' ? 'active' : '' %>">
            <%= t('nav.menu_job') %>
          </a></li>
        <li><a href="<%= rootPath %><%= locale %>/#finance" class="<%= currentPath === 'finance' ? 'active' : '' %>">
            <%= t('nav.menu_finance') %>
          </a></li>
        <li><a href="<%= rootPath %><%= locale %>/#daily-utilities"
            class="<%= currentPath === 'bmi' ? 'active' : '' %>">
            <%= t('nav.menu_utils') %>
          </a></li>
        <li><a href="<%= rootPath %><%= locale %>/#dev-tools"
            class="<%= currentPath === 'json-formatter' ? 'active' : '' %>">
            <%= t('nav.menu_dev') %>
          </a></li>
      </ul>
    </nav>
    <div style="display: flex; gap: 0.5rem; align-items: center;">
      <a href="<%= rootPath %><%= locale === 'vi' ? 'en' : 'vi' %>/<%= pageUrl %>"
        style="text-decoration: none; font-size: 1.2rem;" title="Switch Language">
        <%= locale==='vi' ? '🇺🇸' : '🇻🇳' %>
      </a>
      <button id="theme-toggle" aria-label="Toggle Dark Mode">
        🌙
      </button>
    </div>
  </div>
</header>