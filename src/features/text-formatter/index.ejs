<!-- title: <%= t('textFormatter.title') %> - AZtomiq -->
<link rel="stylesheet" href="<%= toolConfig._assets.css %>">

<section class="formatter-container">
  <div class="page-header">
    <h1>📝 <%= t(toolConfig.translationKey + '.title' ) %>
        <% if (toolConfig.meta && toolConfig.meta.version) { %>
          <button class="version-badge" id="open-changelog" aria-label="View Changelog">v<%= toolConfig.meta.version %>
              </button>
          <% } %>
    </h1>
    <p>
      <%= t(toolConfig.translationKey + '.desc' ) %>
    </p>
  </div>

  <div class="formatter-content">
    <!-- Input Section -->
    <div class="input-section">
      <div class="section-header">
        <h2>📝 <%= t('textFormatter.form.input_label') %>
        </h2>
        <span class="char-count">
          <%= locale==='vi' ? 'Gốc' : 'Original' %>: <span id="stats-original">0</span>
        </span>
      </div>
      <textarea id="input-text" placeholder="<%= t('textFormatter.form.input_placeholder') %>"></textarea>
    </div>

    <!-- Output Section -->
    <div class="output-section">
      <div class="section-header">
        <h2>✨ <%= locale==='vi' ? 'Kết quả' : 'Result' %>
        </h2>
        <span class="char-count">
          <%= locale==='vi' ? 'Mới' : 'Modified' %>: <span id="stats-formatted">0</span>
        </span>
      </div>
      <textarea id="formatted-output" readonly
        placeholder="<%= t('textFormatter.form.output_placeholder') %>"></textarea>
    </div>
  </div>

  <!-- Actions & Stats Row -->
  <div class="actions-row"
    style="display: flex; gap: 1.5rem; margin-bottom: 2rem; align-items: flex-start; flex-wrap: wrap;">
    <!-- Actions -->
    <div class="actions" style="flex: 1; display: flex; gap: 1rem; flex-wrap: wrap;">
      <button class="btn btn-primary" onclick="formatText()" style="flex: 1; max-width: 200px;">
        🚀 <%= t('textFormatter.form.btn_format') %>
      </button>
      <button class="btn btn-secondary" onclick="copyFormatted()" style="flex: 1; max-width: 200px;">
        📋 <%= t('textFormatter.form.btn_copy') %>
      </button>
      <button class="btn btn-secondary" onclick="clearText()" style="flex: 1; max-width: 200px;">
        🔄 <%= t('textFormatter.form.btn_clear') %>
      </button>
    </div>

    <!-- Quick Stats -->
    <div id="format-stats" class="stats"
      style="min-width: 15px; background: var(--bg-hover); padding: 1rem; border-radius: 8px; border: 1px solid var(--border-color);">
      <div style="font-size: 0.9rem; color: var(--text-muted); text-align: center;">
        <div style="margin-bottom: 4px;">
          <%= locale==='vi' ? 'Giảm bớt' : 'Reduced' %>
        </div>
        <div style="font-size: 1.2rem; line-height: 1.2;">
          <strong id="stats-reduction" style="color: var(--success-color);">0</strong> chars
          <div style="font-size: 0.8em; opacity: 0.8;">(<span id="stats-percent">0%</span>)</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Options Grid -->
  <div class="options-section">
    <h3>
      <%= t('textFormatter.form.options_title') %>
    </h3>
    <div class="options-grid">
      <label class="checkbox-container">
        <input type="checkbox" id="remove_extra_spaces" checked>
        <span>
          <%= t('textFormatter.form.remove_extra_spaces') %>
        </span>
      </label>
      <label class="checkbox-container">
        <input type="checkbox" id="remove_line_breaks">
        <span>
          <%= t('textFormatter.form.remove_line_breaks') %>
        </span>
      </label>
      <label class="checkbox-container">
        <input type="checkbox" id="trim_lines" checked>
        <span>
          <%= t('textFormatter.form.trim_lines') %>
        </span>
      </label>
      <label class="checkbox-container">
        <input type="checkbox" id="normalize_quotes" checked>
        <span>
          <%= t('textFormatter.form.normalize_quotes') %>
        </span>
      </label>
    </div>

    <div class="case-transform">
      <label for="case_transform">
        <%= t('textFormatter.form.case_transform') %>
      </label>
      <select id="case_transform">
        <option value="none">
          <%= t('textFormatter.form.case_none') %>
        </option>
        <option value="lowercase">
          <%= t('textFormatter.form.case_lowercase') %>
        </option>
        <option value="uppercase">
          <%= t('textFormatter.form.case_uppercase') %>
        </option>
        <option value="capitalize">
          <%= t('textFormatter.form.case_capitalize') %>
        </option>
        <option value="title">
          <%= t('textFormatter.form.case_title') %>
        </option>
      </select>
    </div>
  </div>

  <!-- Guide Section -->
  <% if (toolConfig.guide) { %>
    <div class="card" style="margin-top: 2rem;">
      <h3 style="margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
        📘 <%= t(toolConfig.guide.title) %>
      </h3>
      <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1.5rem;">
        <% toolConfig.guide.items.forEach(item=> { %>
          <div style="background: var(--bg-hover); padding: 1rem; border-radius: 8px;">
            <h4 style="margin-bottom: 0.5rem; color: var(--primary-color);">
              <%= t(item.title) %>
            </h4>
            <p style="font-size: 0.9rem; color: var(--text-muted); line-height: 1.5;">
              <%= t(item.content) %>
            </p>
          </div>
          <% }); %>
      </div>
    </div>
    <% } %>
</section>

<script src="<%= toolConfig._assets.js %>"></script>